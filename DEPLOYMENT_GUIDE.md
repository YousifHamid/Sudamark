# الدليل الشامل والنهائي لإطلاق تطبيق "سودمارك - Sudmark" 🚀 (نسخة Railway)

هذا الملف يحتوي على كل ما تحتاجه لإطلاق التطبيق بنجاح باستخدام **Railway** (الخيار الأفضل والأقوى).

---

## ✅ لماذا Railway؟
*   **سيرفرات قوية:** تتحمل ضغط مئات الآلاف من المستخدمين.
*   **توسع تلقائي (Auto-scaling):** السيرفر يكبر ويصغر حسب عدد الزوار.
*   **حماية وأمان:** شهادات SSL وحماية DDoS مجانية.

---

## 🛠️ الخطوة 1: رفع السيرفر على Railway

### 1. تجهيز الكود
أنت بالفعل لديك الكود جاهز. تأكد من رفعه على حسابك في **GitHub**.

### 2. إنشاء المشروع على Railway
1.  ادخل على [railway.app](https://railway.app) وسجل حساباً (يفضل باستخدام GitHub).
2.  اضغط **+ New Project** واختر **Deploy from GitHub repo**.
3.  اختر مستودع المشروع (`sudmark`).
4.  اضغط **Deploy Now**.
5.  سيقوم Railway بسحب الكود وبدء التجهيز.

### 3. إضافة قاعدة البيانات (PostgreSQL)
1.  داخل مشروعك في Railway، اضغط كليك يمين في المساحة الفاضية (أو زر New) واختر **Database**.
2.  اختر **PostgreSQL**.
3.  سيقوم Railway بإنشاء قاعدة بيانات وربطها تلقائياً بمتغير `DATABASE_URL` في مشروعك.

### 4. إعداد متغيرات البيئة (Variables)
1.  اضغط على بطاقة المشروع (المربع الخاص بالتطبيق) ثم اذهب لتبويب **Variables**.
2.  أضف المتغيرات التالية:
    *   `SESSION_SECRET`: اكتب أي نص طويل ومعقد (مثلاً: `my-super-secret-key-123`).
    *   `NODE_ENV`: `production`
    *   (ملاحظة): `DATABASE_URL` موجود تلقائياً، لا تحتاج لإضافته يدوياً.
    *   (هام): اضغط **Redeploy** إذا طلب منك ذلك لتفعيل المتغيرات.

### 5. تجهيز الجداول (Database Schema)
1.  بما أن قاعدة البيانات جديدة وفارغة، نحتاج لإنشاء الجداول.
2.  في Railway، اضغط على تبويب **Settngs** الخاص بالتطبيق.
3.  ابحث عن **Build Command** وضع فيه الأمر التالي:
    ```bash
    npm install && npm run db:push
    ```
    *(هذا سيجعله ينشئ الجداول تلقائياً عند كل تحديث).*

### 6. إنشاء رابط للسيرفر (Domain)
1.  اذهب لتبويب **Settings** ثم قسم **Networking**.
2.  اضغط **Generate Domain** (سيمنحك رابطاً مثل `xxx.up.railway.app`).
3.  أو يمكنك ربط "دومين خاص" (Custom Domain) إذا اشتريت واحداً (مثلاً `sudmark.app`).
4.  ⚠️ **انسخ هذا الرابط**، هذا هو رابط السيرفر العالمي.

---

## 🔗 الخطوة 2: ربط التطبيق بالسيرفر

الآن سنضع رابط Railway في التطبيق.

1.  افتح ملف `eas.json` في المشروع.
2.  عدّل قسم `production` ليصبح كالتالي (استبدل الرابط برابط Railway الخاص بك):

```json
"production": {
  "android": {
    "buildType": "app-bundle"
  },
  "env": {
    "EXPO_PUBLIC_DOMAIN": "رابط-سيرفر-ريلوى-بدون-https-أو-فواصل" 
    // مثال: sudmark-production.up.railway.app
  }
}
```

---

## 📱 الخطوة 3: بناء التطبيق النهائي (Google Play)

1.  افتح التيرمينال (Terminal) في جهازك (VS Code).
2.  نفذ أمر البناء:
    ```bash
    npm run build:android:prod
    ```
3.  سيبدأ Expo في بناء التطبيق في السحابة، وعند الانتهاء سيعطيك رابط تحميل ملف `.aab`.
4.  ارفع هذا الملف على **Google Play Console**.

---

## 🔑 لوحة التحكم (Admin Panel)

بعد النشر، ادخل للوحة التحكم:
*   **الرابط:** `https://رابط-سيرفرك/admin`
*   **الإيميل الافتراضي:** `admin`
*   **كلمة المرور:** `admin123`

---

**مبروك!** تطبيقك الآن يعمل على بنية تحتية عالمية قوية جداً. 🌍🚀

💡 تذكير بسيط: بعد أن تعمل الـ Deploy وتأخذ رابط السيرفر الجديد، لا تنسَ وضعه في ملف eas.json قبل بناء النسخة النهائية للمتجر (.aab). كل هذا مشروح في الدليل.