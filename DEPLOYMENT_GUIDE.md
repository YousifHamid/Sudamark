# الدليل الشامل والنهائي لإطلاق تطبيق "عربتي - Arabaty" 🚀

هذا الملف يحتوي على كل ما تحتاجه لإطلاق التطبيق بنجاح. لقد تم الانتهاء من البرمجة، والآن وقت النشر.

---

## ✅ الحالة الحالية للمشروع (ما تم إنجازه)

1.  **نظام الصور العالمي:** تم تحويل نظام رفع الصور ليعمل بتقنية (Base64)، مما يعني أن الصور ستظهر لجميع المستخدمين فوراً عند رفع السيرفر.
2.  **الملف الشخصي (Profile):** تم إصلاح زر التعديل، ويمكن الآن تغيير الاسم، المدينة، وصورة البروفايل، وتظهر التحديثات فوراً.
3.  **نظام الدفع الذكي:**
    *   أول **50,000 إعلان** في التطبيق مجانية تماماً (لجذب المستخدمين).
    *   بعد ذلك، يطلب التطبيق 10,000 جنيه (أو كوبون مجاني).
    *   نظام الكوبونات مفعل (يمكنك إنشاء أكواد خصم).
4.  **الدول المدعومة:** السودان (الافتراضي)، مصر، السعودية، الإمارات، قطر، أيرلندا، ليبيا، اليمن.
5.  **لوحة التحكم (Admin):** جاهزة لإدارة المستخدمين، السيارات، والمدفوعات.

---

## 🛠️ الخطوة 1: رفع السيرفر (Backend)

بما أن التطبيق يحتاج لقاعدة بيانات ليعمل خارج منزلك، سنستخدم خدمة **Render** المجانية والقوية.

### 1. تجهيز الكود
أنت بالفعل لديك الكود جاهز. تأكد من رفعه على حسابك في **GitHub**.

### 2. إنشاء السيرفر وقاعدة البيانات على Render
1.  ادخل على [dashboard.render.com](https://dashboard.render.com) وسجل حساباً.
2.  اضغط **New +** واختر **PostgreSQL** (قاعدة البيانات).
    *   Name: `arabaty-db`
    *   Region: اختر الأقرب (مثلاً Frankfurt).
    *   Plan: **Free**.
    *   اضغط **Create Database**.
    *   ⚠️ **انسخ رابط "Internal Database URL"** واحتفظ به، سنحتاجه.
3.  اضغط **New +** مرة أخرى واختر **Web Service** (السيرفر).
    *   اربط حساب GitHub واختر مستودع المشروع (`arabaty`).
    *   **Name:** `arabaty-app`
    *   **Runtime:** Node
    *   **Build Command:** `npm install && npm run build`
    *   **Start Command:** `npm start`
    *   انزل لأسفل لقسم **Environment Variables** وأضف التالي:
        *   `DATABASE_URL`: (ضع الرابط الذي نسخته من الخطوة السابقة).
        *   `SESSION_SECRET`: (اكتب أي نص عشوائي طويل ومعقد للأمان).
        *   `NODE_ENV`: `production`
    *   اضغط **Create Web Service**.

⏳ **انتظر قليلاً..** سيقوم Render ببناء السيرفر. عندما ترى علامة خضراء (Live)، انسخ رابط السيرفر من الأعلى (مثلاً: `https://arabaty-app.onrender.com`).

---

## 🔗 الخطوة 2: ربط التطبيق بالسيرفر

الآن بعد أن أصبح لدينا رابط عالمي، سنضعه في التطبيق.

1.  افتح ملف `eas.json` في المشروع.
2.  عدّل قسم `production` ليصبح كالتالي (استبدل الرابط برابط Render الخاص بك):

```json
"production": {
  "android": {
    "buildType": "app-bundle"
  },
  "env": {
    "EXPO_PUBLIC_DOMAIN": "arabaty-app.onrender.com" 
    // ^^^ ضع الرابط هنا بدون https:// أو فواصل
  }
}
```

---

## 📱 الخطوة 3: بناء التطبيق النهائي (AAB)

هذه الخطوة ستخرج لك "الملف الواحد" الذي ترفعه لمتجر جوجل بلاي.

1.  افتح التيرمينال (Terminal) في المشروع.
2.  ننفذ الأمر التالي لبناء نسخة المتجر:
    ```bash
    npm run build:android:prod
    ```
3.  سيسألك Expo عن بعض البيانات (حساب Expo الخاص بك)، وسيبدأ البناء في السحابة.
4.  عند الانتهاء، سيعطيك رابطاً لتحميل ملف بصيغة `.aab`.
    *   هذا هو الملف الذي ترفعه في **Google Play Console**.

---

## 🧪 اختبار سريع (اختياري)

إذا أردت تجربة التطبيق على هاتفك قبل رفعه للمتجر (كنسخة APK عادية):
```bash
npm run build:android:preview
```
هذا سيعطيك ملف `.apk` يمكنك تثبيته وتجربته فوراً، وسيتصل أيضاً بسيرفر Render إذا وضعت الرابط في قسم `preview` داخل `eas.json`.

---

## 🔑 لوحة التحكم (Admin Panel)

بعد رفع السيرفر، يمكنك الدخول للوحة التحكم من أي متصفح:
*   **الرابط:** `https://arabaty-app.onrender.com/admin`
*   **الإيميل الافتراضي:** `arab`
*   **كلمة المرور الافتراضية:** `#%#Ara@26%#%`

(يمكنك تغيير كلمة المرور هذه من داخل قاعدة البيانات لاحقاً للأمان).

---

**مبروك!** بهذا يكون لديك تطبيق كامل، سيرفر يعمل على الإنترنت، وملف جاهز للمتجر. 🚗💨
